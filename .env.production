# Base path used by Vite when building for GitHub Pages.
# Leave as "/" for a user site (https://<user>.github.io).
# Set to "/<repo>/" for a project site (https://<user>.github.io/<repo>/).
VITE_BASE_PATH=/
